<div class="contents row">
  <div class="container">
    <h3>結果を共有してみよう</h3>
    <div class="command">
    <%= form_with(model: @tweet, local: true) do |form| %>
      <%= form.text_area :text, placeholder: "何でも書いてみよう", rows: "10", cols: "40" %>
      <%= form.submit "投稿する" %>
    <% end %>
    </div>
  </div>
</div>